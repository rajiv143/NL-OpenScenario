Vehicle A (tt) is ahead in same lane, starts at 10 m/s then slows to 5 m/s. Vehicle B (a2) is behind in adjacent lane, performs stop-and-go behavior. Vehicle C (sprinter) is behind in adjacent lane, remaining stationary.