# Minimal requirements for Llama CARLA training
# Core dependencies only - install these first

# PyTorch (install separately based on your CUDA version)
# Visit https://pytorch.org/get-started/locally/ for the right command

# After PyTorch is installed, install these:
transformers==4.36.2
datasets==2.14.0
accelerate==0.25.0
peft==0.7.1
scikit-learn==1.3.0
tqdm
numpy
sentencepiece
protobuf

# Optional but recommended
bitsandbytes==0.41.3  # For 4-bit quantization (requires CUDA)
wandb  # For experiment tracking